# Product Requirement Document (PRD) - 웹 기반 전화번호 관리 시스템

이 문서는 웹 기반의 간단하고 직관적인 전화번호 관리 시스템(연락처 관리 시스템)을 개발하기 위한 요구사항을 정의합니다.

---

## 1. 프로젝트 개요 (Overview)
- **프로젝트 명**: 웹 기반 전화번호 관리 시스템 (Phonebook Web Application)
- **목적**: 사용자가 브라우저 상에서 본인의 연락처 정보(이름, 전화번호, 이메일, 메모)를 간편하게 관리(조회, 등록, 수정, 삭제)할 수 있는 싱글 페이지 애플리케이션(SPA) 구축
- **핵심 목표**: 별도의 로그인 과정 없이 쉽고 빠른 연락처 CRUD 기능 제공 및 현대적인 프리미엄 디자인(다크 모드, 부드러운 마이크로 애니메이션 등) 적용

---

## 2. 주요 제약 사항 및 기술 스택 (Constraints & Tech Stack)
- **데이터베이스**: Supabase (PostgreSQL 기반, REST API 호출 활용)
- **웹 서버 / 호스팅**: Vercel
- **프론트엔드 아키텍처**: HTML5, Vanilla JavaScript (ES6+), Vanilla CSS
  - 프레임워크나 Tailwind CSS 등 외부 스타일 라이브러리 배제(요청 시에만 도입)하여 순수 Vanilla 코딩으로 경량성 및 통제성 극대화
- **사용자 인증 (로그인)**: 미구현 (요구사항 반영: 로그인은 필요하지 않음)
- **기능 범위**: 필수적인 핵심 CRUD에 집중하여 빠르고 직관적인 기능 위주로 구성

---

## 3. 기능적 요구사항 (Functional Requirements)

### 3.1. 연락처 조회 및 실시간 검색 (Read & Search)
- **목록 뷰**: 데이터베이스에 저장된 전체 연락처 리스트를 세련된 글래스모피즘(Glassmorphism) 카드 또는 깔끔한 리스트 형태의 뷰로 화면에 출력합니다.
- **실시간 검색**: 이름(Name) 또는 전화번호(Phone Number) 입력 시 즉시 매칭되는 리스트를 화면에 필터링해주는 라이브 검색 창을 상단에 제공합니다.
- **정렬**: 한글/영문 가나다순으로 자동 정렬하여 가독성을 보장합니다.

### 3.2. 신규 연락처 등록 (Create)
- **입력 양식**: '새 연락처 추가' 버튼을 눌렀을 때, 동적인 입력 폼(또는 슬라이드 모달)이 부드럽게 나타납니다.
- **입력 필드**:
  - **이름**: 필수 입력 (유효성 검증: 최소 2자 이상)
  - **전화번호**: 필수 입력 (유효성 검증: 전화번호 형식 체크, 입력 시 자동 대시`-` 추가 등 UX 개선 기능 포함)
  - **이메일**: 선택 입력 (유효성 검증: 이메일 정규식 통과 여부 확인)
  - **메모**: 선택 입력 (메모 사항을 적을 수 있는 간단한 한 줄 텍스트 입력창)
- **저장**: 입력값 유효성 검사를 마친 뒤 Supabase API를 통해 실시간으로 DB에 저장하고 목록을 갱신합니다.

### 3.3. 연락처 수정 (Update)
- **수정 진입**: 각 연락처 카드 우상단 또는 하단에 위치한 '수정' 아이콘/버튼을 누릅니다.
- **동적 수정**: 선택한 연락처 정보가 기존 폼에 자동으로 채워지며, 수정 모드로 전환됩니다.
- **반영**: 수정을 완료하고 저장하면 Supabase에 해당 행(Row)을 업데이트하고, 즉각 화면에 애니메이션과 함께 반영됩니다.

### 3.4. 연락처 삭제 (Delete)
- **삭제 진입**: 각 연락처 카드 내 '삭제' 버튼을 누릅니다.
- **확인 팝업**: 실수로 삭제하는 상황을 방지하기 위해 직관적이고 세련된 삭제 경고 컨펌창을 띄웁니다.
- **삭제 수행**: 사용자가 확인을 누를 시 Supabase에서 해당 레코드를 삭제하고, 삭제 완료 Toast 피드백 알림과 함께 목록에서 연락처 카드를 스무스하게 제거합니다.

---

## 4. 비기능적 요구사항 (Non-functional Requirements)
- **프리미엄 디자인 및 반응형 웹 (Responsive & Premium Design)**:
  - 데스크톱 모니터뿐만 아니라 모바일, 태블릿 기기에서도 최적화되어 보이는 완전한 반응형 디자인
  - 기본 다크 테마(Curated HSL dark palette)를 적용하고, 둥근 모서리, 미세한 그림자, 그라데이션 및 유리 질감 효과(Glassmorphism)를 사용해 세련된 시각경험 제공
- **부드러운 사용자 경험 (Smooth UX)**:
  - 데이터 통신 중(로딩)일 때 Skeleton UI 또는 세련된 Spinner를 띄워 답답함 최소화
  - 연락처 생성, 수정, 삭제 등 주요 상호작용 성공 시 화면 하단에 2.5초간 머무는 Toast notification 알림
- **입력값 정제 (Sanitization & Validation)**:
  - XSS 등 악성 스크립트 삽입을 방지하기 위한 HTML escaping 처리
  - 빈 칸 등록을 차단하는 클라이언트 및 DB 레이어의 필수 제약조건 설정

---

## 5. 데이터베이스 설계 (Database Schema)

Supabase `contacts` 테이블 구성 모델:

| 컬럼명 | 데이터 타입 | 제약 조건 | 설명 |
| :--- | :--- | :--- | :--- |
| `id` | `UUID` | PRIMARY KEY, Default: `gen_random_uuid()` | 연락처별 고유 식별자 |
| `name` | `VARCHAR(100)` | NOT NULL | 연락처 소유자 이름 |
| `phone_number` | `VARCHAR(30)` | NOT NULL | 연락처 전화번호 |
| `email` | `VARCHAR(255)` | NULL | 이메일 주소 (선택 입력) |
| `notes` | `TEXT` | NULL | 연락처 메모 사항 (선택 입력) |
| `created_at` | `TIMESTAMPTZ` | Default: `NOW()` | 생성일시 |

---

## 6. 배포 및 환경 설정 (Deployment & Env)
- **Vercel 배포**: Github 저장소와 연동하여 커밋 푸시 시 자동 빌드 및 배포 처리
- **환경 변수 관리**:
  - `SUPABASE_URL` 및 `SUPABASE_ANON_KEY`를 클라이언트 스크립트에 안전하게 구성(또는 Vercel 환경 변수로 관리)
